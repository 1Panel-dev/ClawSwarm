# Web Client 本地开发默认读取这个文件。
# 因为 package.json 的 dev 脚本已经改成了 `vite --mode dev`，
# 所以这里的变量会在 `npm run dev` 时自动生效。

# Scheduler Server 开发联调地址
VITE_API_BASE_URL=http://172.16.200.119:18080
VITE_MESSAGE_TRANSPORT=websocket

# 消息模块默认走真实后端接口。
# 如果后面只想临时看纯前端假数据，再改回 true 即可。
VITE_MESSAGE_MOCK=false
